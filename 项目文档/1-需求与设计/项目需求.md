完善源代码, 我需要再win10下做个APP, 功能如下:

1. 选中一个源目录后, 它会将此目录及子目录下所有的pdf 全部罗列处理. 每个文件后面都有单个的转化进度.
2. 选择或新建一个目标, 会将转化好的md文件全部放在此目录下,不需要按照原文件架构重建目录. 并在APP界面显示
3. APP状态栏显示转化总的进度,总文件数量,已转文件数量.及错误提醒,有错误及时停止
4. 如果有错误停止后.重新开始转化时, 需要检查是否已转化过, 可以接着转换.
5. 如有其他待补充.

首次问题:

1. 选择源目录后,扫描PDF文件时无响应. 扫描PDF,后并将扫描出的pdf罗列到左边源目录文件列表中,
2. 把这个APP页面左右两边的框来区分源文件及生成md文件,一一对应,文件名相同

二次修复测试问题:

1. APP在任务栏下面再增加实时日志,此日志可以COPY,导出,以便可以分析.
2. 为什么转化后的MD文件栏中的状态及进度没有任何改变.
3. 另外增加一个横栏,显示转化结果: 成功,错误等文件详情

增加功能:

1. 启动时检查是否有老进程存在,并提醒是否关闭并开启新窗口.
2. 转化成md的文件必须有源pdf文件的元数据及原始绝对目录,以便可以追溯.
3. 要实现真正的PDF解析,转化成md, 不要偷懒,不需要糊弄人的MD.
4. 如果是相同的源目录,相同的目标目录,如果重新转换时,可以把目标目录的已转换过的文件覆盖掉.



1. 增加APP版本机制.
2. 之前的APP要求让您对图片有些误解了. 我的意思是是部分在PDF的元素本身就是一个图片形式,那就直接图片插入到md文件中. 并不是然您把每一页都把他变成图片,然后解析.
3. 为了提高APP的速度, 建议在运行时将显卡及CPU均调用起来来计算.
