# 📦 PDF-MD-TOOLS 依赖 v2.1

# PDF解析
PyMuPDF>=1.23.0
pdfplumber>=0.10.0

# OCR支持（可选，用于扫描版PDF）
pytesseract>=0.3.10

# Office文档解析
python-docx>=1.0.0      # Word文档 (.docx)
python-pptx>=0.6.21     # PowerPoint文档 (.pptx)
openpyxl>=3.1.0         # Excel文档 (.xlsx)
pywin32>=306            # Windows COM自动化（旧格式.doc/.ppt/.xls转换）

# 图像处理
Pillow>=10.0.0

# 文本处理
regex>=2023.0.0

# GUI框架
customtkinter>=5.2.0

# CLI（可选）
click>=8.1.0
rich>=13.0.0

# 开发依赖
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.1.0
